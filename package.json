{
  "name": "design-tokens",
  "version": "0.1.0",
  "description": "",
  "main": "build.js",
  "files": [
    "build",
    "src"
  ],
  "scripts": {
    "build": "node ./build.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "style-dictionary": "2.8.1"
  }
}
